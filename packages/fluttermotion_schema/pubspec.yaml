name: fluttermotion_schema
description: The FlutterMotion document format -- parsing and validation, with no Flutter dependency.
version: 0.1.0
publish_to: none # until the licensor entity and pub.dev strategy are settled

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
