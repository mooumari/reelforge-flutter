name: reelforge_schema
description: The ReelForge document format -- parsing and validation, with no Flutter dependency.
version: 0.1.1
repository: https://github.com/mooumari/reelforge-flutter

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
