name: reelforge_json
description: Describe a ReelForge composition in JSON and build it at runtime.
version: 0.1.0
repository: https://github.com/mooumari/reelforge

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  reelforge: ^0.1.0
  reelforge_kit: ^0.1.0
  reelforge_schema: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
