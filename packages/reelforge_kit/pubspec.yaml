name: reelforge_kit
description: Ready-made scenes, charts and motion primitives for ReelForge compositions.
version: 0.2.0
repository: https://github.com/mooumari/reelforge-flutter

environment:
  sdk: ^3.9.0
  flutter: ">=3.41.0"

dependencies:
  flutter:
    sdk: flutter
  reelforge: ^0.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
