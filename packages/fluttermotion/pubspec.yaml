name: fluttermotion
description: Build videos with Flutter. Use any Flutter widget as motion graphics, preview them instantly, and render them anywhere.
version: 0.1.0
repository: https://github.com/mooumari/fluttermotion

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
