name: fluttermotion_json
description: Describe a FlutterMotion composition in JSON and build it at runtime.
version: 0.1.0
repository: https://github.com/mooumari/fluttermotion

environment:
  sdk: ^3.9.0
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
  fluttermotion: ^0.1.0
  fluttermotion_kit: ^0.1.0
  fluttermotion_schema: ^0.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
